spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/wikimedia-recent-changes
    username: postgres
    password: postgres
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
